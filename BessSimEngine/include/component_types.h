#pragma once

namespace Bess::SimEngine {
    enum class ComponentType {
        INPUT,
        OUTPUT,
        AND,
        OR,
        NOT,
        NOR,
        NAND,
        XOR,
        XNOR
    };
}
