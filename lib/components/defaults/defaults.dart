part of components;

class Defaults {
  static const DigitalState nandGateOutputState = DigitalState.low;
  static const DigitalState norGateOutputState = DigitalState.low;
  static const DigitalState notGateOutputState = DigitalState.low;
  static const DigitalState pinState = DigitalState.low;
  static const double pinSize = 3.0;
  static const double wireSize = pinSize;
  static const Offset defaultGateSize = Offset(110, 100);
  static const Offset notGateSize = Offset(60, 50);
}
