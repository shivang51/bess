#pragma once

namespace Bess::Simulator {
    enum class ComponentType {
        gate,
        inputSlot,
        outputSlot,
        connection,
        inputProbe,
        outputProbe,
    };
}
